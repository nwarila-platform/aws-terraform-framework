name: acc baseline

on:
  push:
    branches: [acc/wo-01a]

permissions: {}

jobs:
  template-drift:
    permissions:
      contents: read
      pull-requests: write # Required by the released runner; reusable workflows cannot elevate caller permissions.
    uses: nwarila-platform/workflow-template-drift/.github/workflows/check.yaml@d20e0fd946ed87001b582ac88d868a9b45cde839 # v2.1.0
