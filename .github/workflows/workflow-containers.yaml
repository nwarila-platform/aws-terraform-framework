name: Workflow containers
on:
  pull_request:
  push:
    branches: [main]
permissions: {}
jobs:
  template-drift:
    permissions:
      contents: read
      security-events: write # Propagates A14: a nested reusable workflow cannot elevate its caller's token.
    uses: nwarila-platform/workflow-template-drift/.github/workflows/check.yaml@141a77a03da55254c280c4c3a05717fd97ea2789 # acc W0
    with:
      # renovate: datasource=docker depName=ghcr.io/nwarila-platform/workflow-template-drift
      version: 2.1.0
      digest: sha256:7c508dd855042863e6a1fa9ff0fcc6b44694d73f35030c7ec6032dc40e84b093
