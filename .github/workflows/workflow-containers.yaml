name: Workflow containers
on:
  pull_request:
  push:
    branches: [main]
permissions: {}
jobs:
  template-drift:
    permissions:
      contents: read
      security-events: write # Propagates A14: a nested reusable workflow cannot elevate its caller's token.
    uses: nwarila-platform/workflow-template-drift/.github/workflows/check.yaml@e4bd57446213860dfeee052950e9ba1dc6ce1c43 # acc W1
    with:
      # renovate: datasource=docker depName=ghcr.io/nwarila-platform/workflow-template-drift
      version: 2.1.0
      digest: sha256:92fa32bf5cd9ba48adf95bc984677964c21b254e514f45515ea7f13a145b208d
